#ifndef MosaicMorph_H
#define MosaicMorph_H

#include "SPIController.h"

void MosaicMorph_setup(SPIController& spiController);
void MosaicMorph_loop(SPIController& spiController);

#endif // MosaicMorph_H
